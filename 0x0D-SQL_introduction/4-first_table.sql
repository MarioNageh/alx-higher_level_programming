-- CREATE First Table `first_table` in `hbtn_0c_0` Database
CREATE TABLE IF NOT EXISTS `first_table` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `name` VARCHAR(256) NOT NULL,
    PRIMARY KEY (`id`)
);
